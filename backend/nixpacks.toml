# Configuración para Railway (detecta Python y usa este build)
[phases.setup]
nixPkgs = ["python312", "mariadb"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py collectstatic --noinput --clear || true"]

[start]
cmd = "bash start.sh"
